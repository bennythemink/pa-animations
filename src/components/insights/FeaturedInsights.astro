---
import Insights from '@components/insights/Insights.astro'

interface Props {
  animate?: boolean
}

const { animate = true } = Astro.props as Props
---

<Insights animate={animate} featuredOnly={true} />
